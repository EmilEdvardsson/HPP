#include <stdio.h>

inline int add(int a, int b) {
  return a + b;
}

int main() {
  int x = 10;
  int y = 20;
  for (int i = 0; i < 1e6; i++){
  int z = add(x, y);
  }
  printf("%d\n", z);
  return 0;
}
